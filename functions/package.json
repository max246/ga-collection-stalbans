{
  "name": "home-assistant-functions",
  "description": "aaa",
  "license": "Apache-2.0",
  "private": true,
  "engines": {
    "node": "8"
  },
  "dependencies": {
    "actions-on-google": "^2.9.1-preview.1",
    "firebase-admin": "~11.4.1",
    "firebase-functions": "^3.24.1",
    "node-fetch": "^2.6.0"
  },
  "scripts": {
    "lint": "./node_modules/.bin/eslint --fix \"**/*.js\"",
    "start": "firebase serve --only functions",
    "deploy": "firebase deploy --only functions",
    "test": "npm run lint"
  },
  "devDependencies": {
    "eslint": "^6.0.1",
    "eslint-config-google": "^0.13.0"
  }
}
